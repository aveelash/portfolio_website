# Portfolio PDF assets

Place your PDF files here so the chat assistant can serve them when recruiters ask:

| File | Purpose |
|------|---------|
| `resume.pdf` | Aveelash's resume |
| `certificate.pdf` | AWS Cloud Foundations certificate |

Until these files exist, the app falls back to the Google Drive links configured in `services/document_handler.py`.
